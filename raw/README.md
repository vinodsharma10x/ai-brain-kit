# Raw

Your drop zone for source material: meeting transcripts, articles worth keeping, podcast notes, exported threads, PDFs.

**Claude never edits or moves files in here.** This is the human layer. Claude reads from it and distills knowledge into `wiki/` pages (say "ingest this" after dropping something).

Organize by source if you like (`raw/meetings/`, `raw/articles/`), or do not organize at all. Both work.
